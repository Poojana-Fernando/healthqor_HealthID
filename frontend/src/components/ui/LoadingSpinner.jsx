import HealthIdLoadingIcon from './HealthIdLoadingIcon'

/** Alias for the branded Health ID logo loader. */
export default function LoadingSpinner(props) {
  return <HealthIdLoadingIcon {...props} />
}
